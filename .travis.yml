language: python
sudo: required
python:
  - "2.7"
addons:
  apt:
    packages:
      - libblas-dev
      - liblapack-dev
      - gfortran
before_install:
  - pip install --upgrade pip
install:
  - pip install -r requirements.txt
  - python setup.py install
